[package]
name = "refactory-shadow-requests"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"

[lib]
name = "shadow_requests"
crate-type = ["cdylib"]

[dependencies]
pyo3.workspace = true
reqwest = { version = "0.12", features = ["blocking", "json"] }
